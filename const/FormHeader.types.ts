import { TextStyle, ViewStyle } from "react-native";

export type FormHeaderProps = {
    currentStep: number;
};

export const formLabels: string[] = ["Farmer Profile", "Land Details", "Flowering Details", "Flowering Details", "Beekeeping Scope"];
