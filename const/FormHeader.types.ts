import { TextStyle, ViewStyle } from "react-native";

export type FormHeaderProps = {
    totalSteps: number;
    currentStep: number;
};

export const formLabels: string[] = ["Farmer Profile", "Land Details"];
